<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- required=true tells Play Store to only deliver this APK to
         devices with watch hardware. The phone module's APK has no
         such constraint, so Play Store routes correctly per device. -->
    <uses-feature android:name="android.hardware.type.watch" android:required="true" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_compass"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Wear OS 3+ standalone app. The phone-side companion still
             needs to be installed for the Data Layer to connect, but the
             watch APK runs on its own. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Listens for DataLayer pushes from the phone-side WatchBridge.
             Path filters match the constants in phone-side WatchBridge.kt
             (PATH_NAV / PATH_STOP). -->
        <service
            android:name=".WearableMessageListener"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/wegwiesel/"
                    android:scheme="wear" />
            </intent-filter>
        </service>

    </application>

</manifest>
